import {StyleSheet} from 'react-native';
import {theme} from './../../../constants/colors';

const styles = StyleSheet.create({
  tabbar: {backgroundColor: theme.colors.blue},
  tabIcon: {width: 18, height: 18, marginTop: 10},
});
export default styles;
